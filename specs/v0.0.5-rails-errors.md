# v0.0.5 Rails Error Adapter

This file intentionally contains no executable contract yet.

The next product slice after release-tag automation will define black-box behavior for an explicit Rails adapter before implementation begins.
